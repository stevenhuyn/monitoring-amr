[project]
name = "my-lib"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["openai>=1.97.0", "python-dotenv>=1.1.1", "selenium>=4.34.2"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
